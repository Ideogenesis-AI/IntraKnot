[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.sdist]
only-include = [
  "src/", "tests/",
  "README.md", "LICENSE",
  "pyproject.toml",
]

[project]
name = "intraknot"
version = "0.1.0"
description = "Intraknot: HPC management for tensor network and AI studies of quantum lattice models"
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Changkai Zhang", email = "changkai.zhang@lmu.de" }]
keywords = [
  "tensor-network", "hpc", "quantum", "lattice", "pytorch",
  "symmetry", "neural-network", "dmrg",
]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Physics",
]
dependencies = [
  "alice>=0.1.0",
  "nicole>=0.3.5",
  "numpy>=2.0",
  "torch>=2.5",
]

[project.optional-dependencies]
test = [
  "pytest>=7.0",
  "hypothesis>=6.0",
  "pytest-benchmark>=4.0",
]

lint = [
  "ruff>=0.6",
  "mypy>=1.8",
]

[tool.uv.sources]
alice = { path = "../alice", editable = true }
nicole = { path = "../nicole", editable = true }

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-ra -q --strict-markers"
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
  "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
check_untyped_defs = true
disallow_untyped_defs = false
warn_unused_ignores = true
warn_redundant_casts = true
warn_unreachable = true
show_error_codes = true
